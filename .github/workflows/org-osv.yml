name: org-osv

on:
  push:
    branches:
      - "**"
  pull_request:
  workflow_dispatch:

permissions: read-all

jobs:
  osv:
    runs-on: ubuntu-latest
    steps:
      - name: Skip duplicate OSV wrapper
        run: |
          echo "Repository security coverage already runs in Security Audit."
          echo "This org-level wrapper is kept as a compatibility check only."
