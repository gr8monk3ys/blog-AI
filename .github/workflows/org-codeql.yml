name: org-codeql

on:
  push:
    branches:
      - "**"
  pull_request:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  codeql:
    uses: gr8monk3ys/github/.github/workflows/reusable-codeql.yml@f5ea73a3d864afd83314aa96ea37306c0a0f16e1
