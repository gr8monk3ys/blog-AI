# Core dependencies
# Versions aligned with pyproject.toml
tenacity>=8.2.0
openai>=2.15.0
anthropic>=0.76.0
google-generativeai>=0.3.1
requests>=2.31.0
pydantic>=2.5.2,<3.0.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Image generation
stability-sdk>=0.8.0
Pillow>=12.1.1

# Backend server
fastapi>=0.115.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
websockets>=12.0
python-multipart>=0.0.9
uvloop>=0.19.0; platform_system != "Windows"

# Postgres driver (Neon / managed Postgres)
asyncpg>=0.29.0

# Security
bcrypt>=4.0.0,<5.0.0
nh3>=0.2.14

# SSO (Single Sign-On) dependencies
python3-saml>=1.16.0  # SAML 2.0 support
authlib>=1.3.0  # OIDC/OAuth2 support
httpx>=0.27.0,<1.0.0  # Async HTTP client for OIDC
PyJWT>=2.8.0  # JWT token validation
cryptography>=42.0.0,<44.0.0  # Certificate handling

# Redis for job storage
redis>=5.0.0

# Payment processing
stripe>=7.0.0

# Monitoring and error tracking
sentry-sdk[fastapi]>=1.40.0

# Optional development dependencies
# pytest>=7.4.0
# black>=24.3.0
# isort>=7.0.0
# mypy>=1.5.1
protobuf>=5.29.6
starlette>=0.47.2
