{
  "buildCommand": "BROWSERSLIST_IGNORE_OLD_DATA=1 bun run build",
  "installCommand": "rm -rf node_modules apps/web/node_modules && bun install --frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
