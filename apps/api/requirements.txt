# Core dependencies
# Versions aligned with pyproject.toml
tenacity>=9.1.4
openai>=2.15.0
anthropic>=0.76.0
google-generativeai>=0.3.1
requests>=2.31.0
pydantic>=2.5.2,<3.0.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Image generation (Stability AI uses REST API via httpx, no SDK needed)
Pillow>=12.1.1

# Transitive dependency pins (Dependabot security alerts)
starlette>=0.47.2  # CVE: DoS via multipart/form-data (transitive via fastapi)
protobuf>=5.29.6  # CVE: JSON recursion depth bypass (transitive via google-generativeai)

# Backend server
fastapi>=0.115.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
websockets>=12.0
python-multipart>=0.0.9
uvloop>=0.19.0; platform_system != "Windows"

# Postgres driver (Neon / managed Postgres)
asyncpg>=0.29.0

# Security
bcrypt>=4.0.0,<5.0.0
nh3>=0.2.14

# SSO (Single Sign-On) dependencies
python3-saml>=1.16.0  # SAML 2.0 support
authlib>=1.7.0  # OIDC/OAuth2 support
httpx>=0.27.0,<1.0.0  # Async HTTP client for OIDC
PyJWT>=2.12.1  # JWT token validation
cryptography>=46.0.7,<47.0.0  # CVE fixes + certificate handling

# Redis for job storage
redis>=5.0.0

# Payment processing
stripe>=7.0.0

# Monitoring and error tracking
sentry-sdk[fastapi]>=1.40.0

# Optional development dependencies
# pytest>=7.4.0
# pytest-asyncio>=1.3.0
# black>=24.3.0
# isort>=7.0.0
# mypy>=1.5.1
